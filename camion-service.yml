spring:
  datasource:
    url: jdbc:mysql://localhost:3306/camionservice
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE

server:
  port: 8082